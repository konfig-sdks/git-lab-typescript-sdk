/*
GitLab API

An OpenAPI definition for the GitLab REST API.
Few API resources or endpoints are currently included.
The intent is to expand this to match the entire Markdown documentation of the API:
<https://docs.gitlab.com/ee/api/>. Contributions are welcome.

When viewing this on gitlab.com, you can test API calls directly from the browser
against the `gitlab.com` instance, if you are logged in.
The feature uses the current [GitLab session cookie](https://docs.gitlab.com/ee/api/index.html#session-cookie),
so each request is made using your account.

Instructions for using this tool can be found in [Interactive API Documentation](https://docs.gitlab.com/ee/api/openapi/openapi_interactive.html)


The version of the OpenAPI document: v4


NOTE: This file is auto generated by Konfig (https://konfigthis.com).
*/

import { AxiosRequestConfig } from "axios";
import {
  AccessRequestsApi,
  AdminApi,
  AlertManagementApi,
  ApplicationApi,
  ApplicationsApi,
  AvatarApi,
  BadgesApi,
  BatchedBackgroundMigrationsApi,
  BranchesApi,
  BroadcastMessagesApi,
  BulkImportsApi,
  CiVariablesApi,
  ClustersApi,
  JobsApi,
  MetadataApi,
  MigrationsApi,
  PlanLimitsApi,
} from "./api";
import { Configuration, ConfigurationParameters } from "./configuration";
import { GitLabCustom } from "./client-custom";

export class GitLab extends GitLabCustom {
  readonly accessRequests: AccessRequestsApi;
  readonly admin: AdminApi;
  readonly alertManagement: AlertManagementApi;
  readonly application: ApplicationApi;
  readonly applications: ApplicationsApi;
  readonly avatar: AvatarApi;
  readonly badges: BadgesApi;
  readonly batchedBackgroundMigrations: BatchedBackgroundMigrationsApi;
  readonly branches: BranchesApi;
  readonly broadcastMessages: BroadcastMessagesApi;
  readonly bulkImports: BulkImportsApi;
  readonly ciVariables: CiVariablesApi;
  readonly clusters: ClustersApi;
  readonly jobs: JobsApi;
  readonly metadata: MetadataApi;
  readonly migrations: MigrationsApi;
  readonly planLimits: PlanLimitsApi;

  constructor(configurationParameters: ConfigurationParameters) {
    super(configurationParameters);
    const configuration = new Configuration(configurationParameters);
    this.accessRequests = new AccessRequestsApi(configuration);
    this.admin = new AdminApi(configuration);
    this.alertManagement = new AlertManagementApi(configuration);
    this.application = new ApplicationApi(configuration);
    this.applications = new ApplicationsApi(configuration);
    this.avatar = new AvatarApi(configuration);
    this.badges = new BadgesApi(configuration);
    this.batchedBackgroundMigrations = new BatchedBackgroundMigrationsApi(configuration);
    this.branches = new BranchesApi(configuration);
    this.broadcastMessages = new BroadcastMessagesApi(configuration);
    this.bulkImports = new BulkImportsApi(configuration);
    this.ciVariables = new CiVariablesApi(configuration);
    this.clusters = new ClustersApi(configuration);
    this.jobs = new JobsApi(configuration);
    this.metadata = new MetadataApi(configuration);
    this.migrations = new MigrationsApi(configuration);
    this.planLimits = new PlanLimitsApi(configuration);
  }

}
